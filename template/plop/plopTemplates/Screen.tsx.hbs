import React from 'react'
import {Text, View} from 'react-native'

import {AppContainer} from '@/components'
import {useCommonHooks} from '@/hooks'

import use{{name}} from './hooks/use{{name}}'
import {myStyles} from './{{name}}.styles'
import type { {{name}}Props } from './types/{{name}}.types'

const {{name}} = (props: {{name}}Props) => {
  const {colors, RS, t} = useCommonHooks()
  const styles = myStyles(colors, RS)
  const {} = use{{name}}(props)

  return (
    <AppContainer>
      <View style={styles.container}>
        <Text style={styles.textStyle}>{t('{{camelCase name}}.welcome')}</Text>
      </View>
    </AppContainer>
  )
}

export default {{name}}
