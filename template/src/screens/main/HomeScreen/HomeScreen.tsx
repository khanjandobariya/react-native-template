import {AppContainer, AppInnerContainer, AppText} from '@/components'

const HomeScreen = () => {
  return (
    <AppContainer>
      <AppInnerContainer>
        <AppText variant="h1">Home</AppText>
      </AppInnerContainer>
    </AppContainer>
  )
}

export default HomeScreen
